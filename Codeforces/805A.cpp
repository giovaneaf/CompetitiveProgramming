#include <bits/stdc++.h>

using namespace std;

int main() {
    int l, r;
    scanf("%d %d", &l, &r);
    if(l == r) {
        printf("%d", l);
    } else {
        printf("2");
    }
    putchar('\n');
    return 0;
}